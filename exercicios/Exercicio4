import java.util.Scanner;
public class LerSalario {
    public static void main(String[] args) {
         Scanner ler = new Scanner(System.in);
        System.out.println ("Digite o seu salário")
        int salarioUsuario = Scanner(System.in);
        int salarioMinimo =  1320;
        int quantidadeDeSalarios = salarioUsuario / salarioMinimo;

         System.out.println("O usuário ganha aproximadamente " + quantidadeSalariosMinimos + " salários mínimos.");
         scanner.close();
    }
}
   
    
